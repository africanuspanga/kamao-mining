# Optional third-party contact form endpoint.
# If left empty, the contact form falls back to the visitor's email client.
NEXT_PUBLIC_CONTACT_FORM_ENDPOINT=

# Fallback contact email used by the mailto fallback.
NEXT_PUBLIC_CONTACT_EMAIL=info@kamaomining.com
